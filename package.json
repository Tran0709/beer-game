{
  "name": "beer-game-relay-server",
  "version": "1.0.0",
  "description": "Minimal WebSocket relay for The Beer Game (LA4) multiplayer rooms. Relays messages between clients in the same room; holds no game state itself.",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "dependencies": { "ws": "^8.18.0" }
}
