{
  "name": "the-beer-game-la4-relay-server",
  "version": "1.0.0",
  "description": "Tiny WebSocket relay server for The Beer Game (LA4) multiplayer rooms. Does no game logic — only forwards messages between clients in the same room code.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.21.1"
  },
  "license": "MIT"
}
